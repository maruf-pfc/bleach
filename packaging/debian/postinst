#!/bin/bash
set -e

# Install python dependencies
# Note: In a strict debian package, dependencies should be other deb packages (python3-textual, etc)
# But for this standalone distribution request, we might brute force it or assume users use pip.
# Ideally we should bundle the deps or use a PEX/Shiv.

# For this MVP, let's warn user or try to install deps if allowed.
# A proper debian package should NOT use pip at runtime.

echo "Bleach installed."
echo "Note: Ensure 'textual' and 'typer' are installed: pip install textual rich typer"
